# Database (Required)
# Use Neon, Supabase, Railway, or local PostgreSQL
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require"

# App URL (Required for SEO)
# Used for generating shortened URLs and SEO metadata
NEXT_PUBLIC_BASE_URL="https://your-domain.vercel.app"

# Cleanup API Secret (Required for Vercel cron job)
# Generate with: openssl rand -base64 32
# Must match the value set in Vercel Dashboard
CRON_SECRET="your-cron-secret"

# Auth (Optional - for future NextAuth integration)
# NEXTAUTH_SECRET="your-secret-key"
# NEXTAUTH_URL="https://your-domain.com"
