# Database (Required)
# Use Neon, Supabase, Railway, or local PostgreSQL
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require"

# App URL (Optional)
# Used for generating shortened URLs
# On Vercel: automatically uses VERCEL_URL if not set
# Locally: defaults to http://localhost:3000
# BASE_URL="https://your-custom-domain.com"

# Auth (Optional - for future NextAuth integration)
# NEXTAUTH_SECRET="your-secret-key"
# NEXTAUTH_URL="https://your-domain.com"
